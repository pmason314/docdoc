{
  "name": "python-docstring-generator",
  "displayName": "Python Docstring Generator",
  "description": "Generate and update Python docstrings in Google, NumPy, or Sphinx format with optional AI assistance.",
  "version": "0.1.0",
  "publisher": "pmason314",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pmason314/docdoc"
  },
  "engines": {
    "vscode": "^1.104.0"
  },
  "categories": [
    "Programming Languages",
    "Formatters",
    "Other"
  ],
  "keywords": [
    "python",
    "docstring",
    "documentation",
    "google",
    "numpy",
    "sphinx"
  ],
  "activationEvents": [
    "onLanguage:python"
  ],
  "main": "./dist/extension.js",
  "type": "module",
  "contributes": {
    "commands": [
      {
        "command": "docstringGenerator.generate",
        "title": "Python: Generate Docstring"
      },
      {
        "command": "docstringGenerator.generateFile",
        "title": "Python: Generate Docstrings for File"
      },
      {
        "command": "docstringGenerator.update",
        "title": "Python: Update Docstring"
      },
      {
        "command": "docstringGenerator.updateFile",
        "title": "Python: Update All Docstrings in File"
      },
      {
        "command": "docstringGenerator.convertFormat",
        "title": "Python: Convert Docstring Format"
      },
      {
        "command": "docstringGenerator.convertFileFormat",
        "title": "Python: Convert All Docstrings in File"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "docstringGenerator.generate",
          "when": "editorLangId == python"
        },
        {
          "command": "docstringGenerator.generateFile",
          "when": "editorLangId == python"
        },
        {
          "command": "docstringGenerator.update",
          "when": "editorLangId == python"
        },
        {
          "command": "docstringGenerator.updateFile",
          "when": "editorLangId == python"
        },
        {
          "command": "docstringGenerator.convertFormat",
          "when": "editorLangId == python"
        },
        {
          "command": "docstringGenerator.convertFileFormat",
          "when": "editorLangId == python"
        }
      ]
    },
    "configuration": {
      "title": "Python Docstring Generator",
      "properties": {
        "docstringGenerator.format": {
          "type": "string",
          "default": "",
          "enum": [
            "",
            "google",
            "numpy",
            "sphinx"
          ],
          "enumDescriptions": [
            "Auto-detect from pyproject.toml; falls back to Google",
            "Google style",
            "NumPy style",
            "Sphinx / reStructuredText style"
          ],
          "description": "Docstring format. If empty, format is auto-detected from pyproject.toml; falls back to Google."
        },
        "docstringGenerator.quoteStyle": {
          "type": "string",
          "default": "double",
          "enum": [
            "double",
            "single"
          ],
          "enumDescriptions": [
            "Use \"\"\" (double quotes)",
            "Use ''' (single quotes)"
          ],
          "description": "Quote style for generated docstrings."
        },
        "docstringGenerator.trigger.tripleQuote": {
          "type": "boolean",
          "default": true,
          "description": "Enable the triple-quote auto-expand trigger."
        },
        "docstringGenerator.trigger.codeAction": {
          "type": "boolean",
          "default": true,
          "description": "Show the lightbulb code action on undocumented functions."
        },
        "docstringGenerator.onSave.enable": {
          "type": "boolean",
          "default": false,
          "description": "Generate docstrings for all undocumented functions and classes on file save."
        },
        "docstringGenerator.includeTypesFromAnnotations": {
          "type": "boolean",
          "default": true,
          "description": "Include type info in the docstring when a type annotation is present in the signature. When false, type fields are omitted on generation and stripped from existing entries on update."
        },
        "docstringGenerator.includeDefaults": {
          "type": "boolean",
          "default": true,
          "description": "Append 'Defaults to X.' in the parameter description when a default value is present."
        },
        "docstringGenerator.includeExtendedSummary": {
          "type": "boolean",
          "default": false,
          "description": "Include an extended summary placeholder section after the one-line summary."
        },
        "docstringGenerator.returns.skipNone": {
          "type": "boolean",
          "default": true,
          "description": "Suppress the Returns section when the return annotation is None or absent."
        },
        "docstringGenerator.returns.requireAnnotation": {
          "type": "boolean",
          "default": true,
          "description": "Only emit a Returns section when a return annotation is present. When skipNone is also true, skipNone takes priority (a -> None annotation is always suppressed)."
        },
        "docstringGenerator.detectGenerators": {
          "type": "boolean",
          "default": true,
          "description": "Emit Yields instead of Returns for generator functions."
        },
        "docstringGenerator.raises.useSubprocess": {
          "type": "boolean",
          "default": true,
          "description": "Use the workspace Python interpreter for precise raises analysis. Falls back to simple scan if unavailable."
        },
        "docstringGenerator.raises.useSimpleScan": {
          "type": "boolean",
          "default": true,
          "description": "Fall back to tree-sitter token scanning for raises when no interpreter is available."
        },
        "docstringGenerator.mergeInitParams": {
          "type": "boolean",
          "default": false,
          "description": "Hoist __init__ parameters into the class-level docstring instead of documenting them on __init__."
        },
        "docstringGenerator.includeClassAttributes": {
          "type": "boolean",
          "default": true,
          "description": "Include an Attributes section for class-level annotated assignments and dataclass fields."
        },
        "docstringGenerator.update.removeStaleParams": {
          "type": "boolean",
          "default": true,
          "description": "Remove parameter entries from the docstring when the parameter no longer exists in the signature. When false, stale entries are left in place."
        },
        "docstringGenerator.placeholders.summary": {
          "type": "string",
          "default": "_summary_",
          "description": "Placeholder text for the summary line when AI generation is disabled."
        },
        "docstringGenerator.placeholders.description": {
          "type": "string",
          "default": "",
          "description": "Placeholder text for parameter and return descriptions. Empty string leaves the field blank."
        },
        "docstringGenerator.pythonPath": {
          "type": "string",
          "default": "",
          "description": "Explicit path to the Python interpreter for AST analysis. If empty, the extension uses the interpreter selected by the Python extension (ms-python.python)."
        },
        "docstringGenerator.ai.generateSummary": {
          "type": "boolean",
          "default": false,
          "description": "Use the VS Code LM API to generate the summary line."
        },
        "docstringGenerator.ai.generateParamDescriptions": {
          "type": "boolean",
          "default": false,
          "description": "Use the VS Code LM API to generate parameter descriptions."
        },
        "docstringGenerator.ai.generateReturnDescription": {
          "type": "boolean",
          "default": false,
          "description": "Use the VS Code LM API to generate the return description."
        },
        "docstringGenerator.ai.includeBodyContext": {
          "type": "boolean",
          "default": false,
          "description": "Send the function body to the LM for richer context."
        },
        "docstringGenerator.ai.maxBodyTokens": {
          "type": "number",
          "default": 500,
          "minimum": 0,
          "description": "Maximum number of tokens from the function body to include in the LM prompt."
        },
        "docstringGenerator.ai.modelFamily": {
          "type": "string",
          "default": "",
          "description": "Pin to a specific LM model family (e.g., 'claude-haiku', 'gpt-4o'). If empty, the best available model is selected automatically."
        }
      }
    }
  },
  "scripts": {
    "compile": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "package": "NODE_ENV=production node esbuild.mjs",
    "vscode:prepublish": "npm run package",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "format": "prettier --write \"src/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.ts\"",
    "test:unit": "mocha",
    "test:integration": "vscode-test",
    "test": "npm run test:unit && npm run test:integration"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.104.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.0",
    "@vscode/vsce": "^3.0.0",
    "c8": "^10.0.0",
    "chai": "^4.5.0",
    "esbuild": "^0.24.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "mocha": "^11.0.0",
    "prettier": "^3.0.0",
    "ts-node": "^10.9.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "smol-toml": "^1.3.0",
    "tree-sitter-python": "^0.25.0",
    "web-tree-sitter": "^0.26.8"
  },
  "lint-staged": {
    "src/**/*.ts": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
